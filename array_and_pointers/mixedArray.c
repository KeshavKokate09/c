/*
 * As the first rule of an array array is an combination of same data typevalues but in c we can add multiple atatypes in c with respect to int only
 */

#include<stdio.h>
void main(){
	int arr[]={10,20.5,'A',30};
	printf("%d",arr[1]);
}
